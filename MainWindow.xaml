<Window x:Class="RightSnip.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RightSnip" Height="510" Width="430"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F4F8FD">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="34" />
            <RowDefinition Height="72" />
            <RowDefinition Height="24" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="28" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="RightSnip"
                   FontFamily="Century Gothic, Segoe UI"
                   FontWeight="Bold"
                   FontSize="32"
                   Foreground="#1269D3" />

        <TextBlock Grid.Row="1"
                   Margin="0,8,0,0"
                   Text="Capture. Save. Paste."
                   FontSize="16"
                   Foreground="#526274" />

        <Button x:Name="RightSnipButton"
                Grid.Row="3"
                Click="RightSnipButton_Click"
                Content="Right Snip"
                FontSize="18"
                FontWeight="SemiBold"
                Foreground="White"
                Background="#1677E8"
                BorderThickness="0"
                Cursor="Hand" />

        <TextBlock x:Name="StatusText"
                   Grid.Row="5"
                   Text="Captures the display containing your mouse pointer."
                   TextWrapping="Wrap"
                   FontSize="14"
                   LineHeight="21"
                   Foreground="#526274" />

        <Button Grid.Row="7"
                Click="InstallContextMenuButton_Click"
                Content="Add Right Snip to Windows right-click menu"
                Height="42"
                FontWeight="SemiBold"
                Foreground="#1269D3"
                Background="White"
                BorderBrush="#B8D5F6"
                Cursor="Hand" />

        <Button Grid.Row="9"
                Click="RemoveContextMenuButton_Click"
                Content="Remove Windows right-click menu entry"
                Height="34"
                Foreground="#526274"
                Background="Transparent"
                BorderBrush="#CCD8E5"
                Cursor="Hand" />
    </Grid>
</Window>
